# Claude Code instructions

Read and follow [`AGENTS.md`](./AGENTS.md) as the canonical instructions for
this repository. It applies equally to Claude Code, Codex, and subagents.

Every task must use a fresh `claude/<short-slug>` branch (or the branch
convention in `AGENTS.md`), run the required checks, and produce a separate PR
without exposing credentials or changing Git identity.
